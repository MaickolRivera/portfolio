export type Project = {
    title: string;
    description: string;
    img: string;
    repository: string;
    link: string;
  };

export type ThemeKey = "light" | "dark";
export type LangKey = "es" | "en";